target-version = "py312"

[lint]
# Keep the repository-wide gate stable across Ruff releases. This is the
# documented default correctness surface, applied to every maintained Python
# file rather than a hand-picked verifier list.
select = ["E4", "E7", "E9", "F"]
