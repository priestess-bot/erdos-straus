# SP-05 complete branch proof package

Primary proof: `SP-05-complete-proof.md`.

This package proves a finite complete terminal decision procedure, the conditional `q=1,G` phase-root successor theorem, and the exact equivalence between a concrete nonterminal edge and an ordinary `q=1,G` Erdős--Straus counterexample plus repository authority.

Supporting contracts and verdicts:

- `TYPE-CONTRACTS.md`
- `VERIFICATION.md`
- `STATUS.md`

Run:

```bash
./run_all.sh
```

The package is not a repository issuer, registry grant, admission, queue mutation or concrete nonterminal-edge receipt. It rejects caller-fabricated actualness even when the object is canonically sealed and uses nonzero digests.
