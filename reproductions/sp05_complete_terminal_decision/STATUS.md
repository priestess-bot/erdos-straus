# SP-05 proof-package status

Pinned public repository baseline:

```text
7dff8a9e7338814e83ab839c33b8b58c28f4ea0d
```

## Established by this package

- finite, terminating and complete terminal decision for each fixed `p`;
- M23 registered-prefix soundness and preemption;
- global sorted factor-pair coverage using `(a y-b)(a z-b)=b^2`;
- unique phase-root projection and canonical anchor MISS;
- equation-level identity lift;
- frozen T5 vectors and `PHASE_DROP` comparison;
- conditional target typing/re-entry theorem under an acyclic admission extension;
- exact equivalence between a concrete complete-terminal-first nonterminal edge and an ordinary `q=1,G` Erdős--Straus counterexample plus repository authority.

## Deliberately not issued

- exact-HEAD source actualness;
- a production complete-schedule registry grant;
- current-V1 successor admission or queue mutation;
- a concrete nonterminal-edge receipt.

The code rejects its own source fixture and also rejects forged, correctly sealed “actualness” objects with nonzero digests. It contains no external Git trust-anchor resolver or repository role issuer.

## Correct proposition status

```text
OPEN_PROPOSITION
```

This status is required if SP-05 means “the first concrete actual nonterminal edge.” The branch-total theorem itself is proved in `SP-05-complete-proof.md`.
