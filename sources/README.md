# 原始来源管理

`sources/files/` 用于本地保存合法获取的论文、补充材料和代码。该目录默认不进
Git，也不进入公开构建。每个取得的文件应在对应论文卡中记录：

- 来源 URL 与取得日期；
- 相对路径与 SHA-256；
- 是否逐项对照过原文；
- 开放许可或仅限本地研究的状态。

网页可访问或二手书目存在，不等于 `source_acquired: true`。只有原始文件已经
落盘时才设置该字段；只有完成对照核查后才设置
`source_verified_against_original: true`。
