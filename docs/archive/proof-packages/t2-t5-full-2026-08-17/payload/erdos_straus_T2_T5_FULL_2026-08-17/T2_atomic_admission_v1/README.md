# T2 Atomic-Admission v1

Status:

```text
T2_ATOMIC_ADMISSION_V1 = PHASE_LOCAL_GRAMMAR_CLOSED
```

T2 freezes a finite, versioned atomic admission grammar.  It does not claim T5/T6 or the
Erdős–Straus conjecture.

The v1 live/conditional arms are:

1. `h4_a1_clean_q_atomic_v1` — actual H4 parent-anchored arm, locally E1--E5 closed.
2. `c8_double_low_parent_atomic_v1` — conditional arm; admission is closed if an actual
   double-low receipt exists, but existence is not a T2 theorem.

Generic atomic schemas, control-only fixtures and standalone stutters are not v1 arms.
