# SP-21 / SP-22 concrete closure overlay

This package targets `priestess-bot/erdos-straus` at commit
`e6e9e4a8c41b90a330b9ef333e542c18c2cb7be4`.

It closes the six concrete SP-21 defects in one exact signed research scope:

1. a canonical eight-action source policy and target policy;
2. an exhaustive registered-terminal overlap/priority partition;
3. an offline external coordinator signature and complete artifact lock;
4. actual parentless root source, occurrence, lineage, policy and branch binding;
5. a standard-library independent prefix/end-to-end replayer that never imports the selected producer;
6. a universal local-totality proof for every source in the signed decidable ordinary q=1,G predicate domain.

The same slice establishes SP-22 for every externally admitted source in that domain. `p=21169` supplies the full positive edge/admission/re-entry trace; the gap-31 solution remains a later-terminal negative control. A dual-implementation bounded audit covers all 606 domain roots below 100000.

## Apply and verify

```bash
python verify_package.py
python apply_overlay.py /path/to/erdos-straus
```

The installer requires the exact base HEAD, copies the overlay, applies an exact root-README patch, regenerates evidence, runs the independent replayer and executes 37 focused tests. Use `--no-verify` only to inspect the changes. The installer is idempotent for the README patch.

## Primary files

```text
repo-overlay/docs/standalone-proof-propositions-2026-08-28/SP-21-scoped-terminal-priority-admissibility.md
repo-overlay/docs/standalone-proof-propositions-2026-08-28/SP-22-q1-scoped-phase-root-actual-edge.md
repo-overlay/data/t6-sp21-q1-p21169/sp21-policy-registry-v1.json
repo-overlay/data/t6-sp21-q1-p21169/sp21-external-authority-anchor-v1.json
repo-overlay/scripts/t6_sp21_q1_p21169_concrete_selector_v1.py
repo-overlay/scripts/t6_sp21_q1_p21169_independent_replayer_v1.py
repo-overlay/reproductions/sp21_q1_p21169_concrete_selector_v1/independent-replay-v1.json
repo-overlay/tests/test_t6_sp21_q1_p21169_concrete_selector_v1.py
```

## Exact status boundary

The overlay marks SP-21 and SP-22 `ESTABLISHED` only in the signed predicate-policy scope. It leaves production-wide SP-03, F1, F2, F3, T6 and the Erdős–Straus conjecture open. The isolated authority does not activate or grant authority to the existing production runtime.

The private signing key used to produce the detached one-shot authority anchor is deliberately absent from this package.
