# T6 F2/F3 Wave1 H4 C1 Boundary Update

**Date:** 2026-08-25

This note reconciles the older H4 capacity-one warning with the later
source-gate exclusion theorem. It is a scope correction, not an F2 or T6
closure receipt.

## Established Empty Domain

The claim
`type-I-h4-atomic-capacity-one-source-gate-exclusion` proves that the exact
following source domain is empty:

```text
actual H4_A1 + proper-overlap + top-capacity + a=1 + clean-q atomic arm
AND canonical target capacity C=1
```

The proof uses the negative-residue source gate `E_x = -q (mod p)`, the
actual 31-phase restriction, and replay of the sole high-parameter survivor.
It is not an inference from the earlier positive-sign stutter theorem.

## Remaining Open Domain

The theorem does not prove any of the following empty:

- non-atomic H4 outputs;
- H4 source domains outside the retained `H4_A1` clean-q atomic arm;
- future producer target sets;
- global terminal-first-surviving high-support `C=1` states.

Those states, if actually admitted, still belong to the F2 high-support
totality problem and require a terminal or a full E1--E5 plus re-entry exit.

## Records Updated

The active wave1 residual frontier, Gate 2 decision, H4 target-owner receipt,
and c8/H4 atomic residual receipts now state this split explicitly. Historical
audits and interface proposals remain evidence of the pre-exclusion concern;
they must be read together with this update.
